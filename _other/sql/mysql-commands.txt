MYSQL COMMANDS :

STATUS : service mysql status
VERSION : mysqladmin -p -u root version
CONN : mysql -u root -p (dbname)
       mysql -u admin -p lovebip lovebip
CREATE USER : CREATE USER 'admin'@'localhost' identified by 'lovebip';
CREATE DB : CREATE DATABASE lovebip;
GRANT : GRANT ALL PRIVILEGES ON lovebip.* TO admin;

DROP : drop database lovebirds;
       drop user 'admin'@'localhost'


CREATE TABLES : 
